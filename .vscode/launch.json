{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Play Game",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8080", // Using 8080 as per vite.config.dev.mjs
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "npm: dev"
        }
    ]
}